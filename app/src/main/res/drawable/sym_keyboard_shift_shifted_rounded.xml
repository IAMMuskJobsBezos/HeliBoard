<?xml version="1.0" encoding="utf-8"?>
<!-- Elderly-phone shipped default: same house/arrow silhouette as sym_keyboard_shift_rounded, but
     solid-filled - shown while shift is active (temporary or locked) so the state change is
     visually obvious, not just a color swap on an identical glyph. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFF"
        android:pathData="M12,3 L21,12 L16,12 L16,19 L8,19 L8,12 L3,12 Z"/>
</vector>
